import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FF5733',
      // Otros ajustes de color...
    },
    secondary: {
      main: '#E0C2FF',
      light: '#F5EBFF',
      // Otros ajustes de color...
    },
  },
  // Otras configuraciones de tipografía, espaciado, etc.
});

export default theme;





